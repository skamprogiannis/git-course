#!/usr/bin/env bash
# Task 01: configure Git on a Zone01 workstation.

git config --global user.name "skamprog"
git config --global user.email "boot.stefan.os@proton.me"
git config --global init.defaultBranch main
